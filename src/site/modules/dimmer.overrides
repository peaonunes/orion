/*******************************
        User Overrides
*******************************/

.ui.dimmer {
  display: flex;
  transition: opacity 0.25s cubic-bezier(0.75, 0, 0.24, 1.01);

  &:not(.active) {
    pointer-events: none;
  }
}
